apiVersion: apps/v1
kind: Deployment
metadata:
  name: messaging-bee
  labels:
    app: messaging-bee
spec:
  selector:
    matchLabels:
      app: messaging-bee
  template:
    metadata:
      labels:
        app: messaging-bee
    spec:
      volumes:
      - name: logs
        hostPath:
          path: /var/log/akingbee

      containers:
      - image: yomain/akingbee:v1.1.1
        name: messaging-bee
        env:
        - name: REDIS_HOST
          value: redis
        - name: SERVICE_NAME
          value: messaging-bee
        - name: IDENTIFIER
          value: AKB
        - name: ENV
          value: PROD
        - name: AKB_NOREPLY_EMAIL_PASSWORD
          valueFrom:
            secretKeyRef:
              name: email-akingbee-noreply
              key: password
        command: ["python", "listener.py"]
        volumeMounts:
        - mountPath: /app/log
          name: logs


